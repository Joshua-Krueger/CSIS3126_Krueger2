CREATE TABLE users(
    user_id int NOT NULL PRIMARY KEY AUTO_INCREMENT,
    name varchar(50) NOT NULL,
    email varchar(40) NOT NULL,
    password varchar(32) NOT NULL,
    trust_rating int DEFAULT '5',
    token int,
    profile_picture varchar(120)
    );

CREATE TABLE tokens(
    token int,
    user_id int
    );
